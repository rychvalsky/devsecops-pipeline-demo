# Development / CI-only dependencies. Installed on top of requirements.txt.
-r requirements.txt

pytest==9.1.1
pytest-cov==6.0.0
httpx==0.28.1
ruff==0.8.4

# SAST (F4). Extras: toml = read config from pyproject.toml, sarif = SARIF output.
bandit[sarif,toml]==1.9.4

# Dependency vulnerability scan (F5).
pip-audit==2.10.1
