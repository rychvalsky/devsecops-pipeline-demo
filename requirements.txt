# Runtime dependencies. Pinned exactly so scans and builds are reproducible.
# Bumped in F9: the old fastapi pin dragged in a starlette with known CVEs.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlmodel==0.0.42
